%!TEX root = ../main/main.tex
% checklist #1
\def \checkA {Name meaningfulness}

% checklist #2
\def \checkB {One char for loops}

% checklist #3
\def \checkC {Classes are nouns in camelCase}

% checklist #4
\def \checkD {Interfaces are nouns in camelCase}

% checklist #5
\def \checkE {Methods are verbs in camelCase}

% checklist #6
\def \checkF {Class variables in camelCase (may be preceded by ``\_'')}

% checklist #7
\def \checkG {Constants all UPPERCASE (with words separated by ``\_'')}

% checklist #8
\def \checkH {Three or four spaces}

% checklist #9
\def \checkI {No tabs}

% checklist #10
\def \checkJ {Consistent bracing style}

% checklist #11
\def \checkK {Single statements sorrounded by curly braces}

% checklist #12
\def \checkL {Blank lines and optional comments to separate sections}

% checklist #13
\def \checkM {Line length <= 80 characters (when practical)}

% checklist #14
\def \checkN {If it must exceed 80 characters, then they are <= 120}

% checklist #15
\def \checkO {Line breaks after comma or operator}

% checklist #16
\def \checkP {Higher-level breaks are used}

% checklist #17
\def \checkQ {New statements aligned with same level expressions}

% checklist #18
\def \checkR {Comments adequately explain the purpose of the code}

% checklist #19
\def \checkS {Commented code has reason to exist and contains date}

% checklist #20
\def \checkT {Each java source file contains only one public class or interface}

% checklist #21
\def \checkU {The public class is the first class or interface in the file}

% checklist #22
\def \checkV {External program interfaces are implemented consistently w.r.t. the javadoc}

% checklist #23
\def \checkW {Javadoc is complete}

% checklist #24
\def \checkX {If any package statements are needed, they should be the first non-comment statements}

% checklist #25
\def \checkY {Class or interface follows the declaration template: {
	\emph{\begin{enumerate}
			\item Class/interface documentation comment
			\item Class/interface statement
			\item Class/interface implementation comment, if necessary
			\item Class (static) variables
			\begin{enumerate}
				\item first \emph{public} class variables
				\item next \emph{protected} class variables
				\item next package level (no access modifier)
				\item last \emph{private} class variables
			\end{enumerate}
			\item Instance variables
			\begin{enumerate}
				\item first \emph{public} class variables
				\item next \emph{protected} class variables
				\item next package level (no access modifier)
				\item last \emph{private} class variables
			\end{enumerate}
			\item Constructors
			\item Methods
		\end{enumerate}}
}}

% checklist #26
\def \checkZ {Methods grouped by functionality}

% checklist #27
\def \checkAA {Code is free of duplicates, long methods, big classes, breaking encapsulation, also, coupling and cohesion are adequate}

% checklist #28
\def \checkAB {Correct type of variables and class memebers (right visibility)}

% checklist #29
\def \checkAC {Variables are declared in the proper scope}

% checklist #30
\def \checkAD {Constructors are called when a new object is desired}

% checklist #31
\def \checkAE {All object references are initialized before use}

% checklist #32
\def \checkAF {Variables are initialized where they are declared, unless dependent upon
a computation}

% checklist #33
\def \checkAG {Declarations appear at the beginnig of blocks}

% checklist #34
\def \checkAH {Parameters are presented in the correct order}

% checklist #35
\def \checkAI {Correct method is being called}

% checklist #36
\def \checkAJ {Method returned variables are used properly}

% checklist #37
\def \checkAK {Required array elements are accessed through the index}

% checklist #38
\def \checkAL {Indexes have been prevented from going out-of-bounds}

% checklist #39
\def \checkAM {Constructors are called when a new array item is desired}

% checklist #40
\def \checkAN {All objects are compared with ``equals''}

% checklist #41
\def \checkAO {Displayed output is free of spelling and grammatical errors}

% checklist #42
\def \checkAP {Error messages are comprehensive and provide guidance as to how to correct the problem}

% checklist #43
\def \checkAQ {Output is formatted correctly in terms of line stepping and spacing}

% checklist #44
\def \checkAR {Implementation avoids ``brutish programming'' \footnote{See \url{http://users.csc.calpoly.edu/~jdalbey/SWE/CodeSmells/bonehead.html}.}}

% checklist #45
\def \checkAS {Order of computation/evaluation, operator precedence and parenthesizing}

% checklist #46
\def \checkAT {Liberal use of parenthesis is used to avoid operator precedence problems.}

% checklist #47
\def \checkAU {All denominators of a division are prevented from being zero}

% checklist #48
\def \checkAV {Integer arithmetic, especially division, are used appropriately
to avoid causing unexpected truncation/rounding}

% checklist #49
\def \checkAW {Comparison and Boolean operators are correct}

% checklist #50
\def \checkAX {Throw-catch expressions, and their error condition is
actually legitimate}

% checklist #51
\def \checkAY {Code is free of any implicit type conversions}

% checklist #52
\def \checkAZ {Relevant exceptions are caught}

% checklist #53
\def \checkBA {Appropriate action are taken for each catch block}

% checklist #54
\def \checkBB {In a switch statement, all cases are addressed by break or return}

% checklist #55
\def \checkBC {All switch statements have a default branch}

% checklist #56
\def \checkBD {All loops are correctly formed, with the appropriate
initialization, increment and termination expressions}

% checklist #57
\def \checkBE {All files are properly declared and opened}

% checklist #58
\def \checkBF {All files are closed properly, even in the case of an error}

% checklist #59
\def \checkBG {EOF conditions are detected and handled correctly}

% checklist #60
\def \checkBH {All file exceptions are caught and dealt with accordingly}
